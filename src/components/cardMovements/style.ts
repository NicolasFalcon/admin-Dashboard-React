import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  content: {
    paddingTop: 10,
    justifyContent: 'center',
  },

  cardContent: {
    textAlign: 'left',
  },
  text: {
    textAlign: 'right',
  },
});
