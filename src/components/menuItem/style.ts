import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  menuItemText: {
    textAlign: 'initial',
    color: '#6100d1',
    fontWeight: 'bold',
    fontSize: '1.5rem',
    fontFamily: 'Montserrat',
  },
});
